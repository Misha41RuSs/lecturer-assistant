name: digital-lecturer-prod
services:
  postgres:
    image: postgres:15-alpine
    container_name: postgres_db
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: root_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./postgres-init/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U user -d root_db"]
      interval: 5s
      timeout: 5s
      retries: 20
      start_period: 10s
    networks:
      - app-network
    restart: unless-stopped

  content-service:
    image: ghcr.io/misha41russ/lecturer-assistant/content-service:${IMAGE_TAG:-latest}
    container_name: content_service
    ports:
      - "8081:8081"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/content_db
      - SPRING_DATASOURCE_USERNAME=user
      - SPRING_DATASOURCE_PASSWORD=password
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - app-network

  lecture-broadcasting-service:
    image: ghcr.io/misha41russ/lecturer-assistant/lecture-broadcasting-service:${IMAGE_TAG:-latest}
    container_name: lecture_broadcasting_service
    ports:
      - "8082:8082"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/broadcasting_db
      - SPRING_DATASOURCE_USERNAME=user
      - SPRING_DATASOURCE_PASSWORD=password
      - TELEGRAM_BOT_TOKEN=${TELEGRAM_BOT_TOKEN}
      - TELEGRAM_BOT_USERNAME=${TELEGRAM_BOT_USERNAME}
      - CONTENT_SERVICE_URL=http://content-service:8081
      - ANALYTICS_SERVICE_URL=http://analytics-service:8084
      - QUIZ_SERVICE_URL=http://quiz-service:8083
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - app-network

  quiz-service:
    image: ghcr.io/misha41russ/lecturer-assistant/quiz-service:${IMAGE_TAG:-latest}
    container_name: quiz_service
    ports:
      - "8083:8083"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/quiz_db
      - SPRING_DATASOURCE_USERNAME=user
      - SPRING_DATASOURCE_PASSWORD=password
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - app-network

  analytics-service:
    image: ghcr.io/misha41russ/lecturer-assistant/analytics-service:${IMAGE_TAG:-latest}
    container_name: analytics_service
    ports:
      - "8084:8084"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/analytics_db
      - SPRING_DATASOURCE_USERNAME=user
      - SPRING_DATASOURCE_PASSWORD=password
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - app-network

  admin-gateway:
    image: ghcr.io/misha41russ/lecturer-assistant/admin-gateway:${IMAGE_TAG:-latest}
    container_name: admin_gateway
    ports:
      - "8080:8080"
    environment:
      - CONTENT_SERVICE_URL=http://content-service:8081
      - LECTURE_BROADCASTING_SERVICE_URL=http://lecture-broadcasting-service:8082
      - QUIZ_SERVICE_URL=http://quiz-service:8083
      - ANALYTICS_SERVICE_URL=http://analytics-service:8084
    depends_on:
      - content-service
      - lecture-broadcasting-service
      - quiz-service
      - analytics-service
    networks:
      - app-network

networks:
  app-network:
    driver: bridge

volumes:
  postgres_data:
